interface:
  display_name: "Freshers Match Overview"
  short_description: "Guardrails for the event matching app"
  brand_color: "#D4916E"
  default_prompt: "Use $freshers-match-overview to orient yourself before changing product scope, architecture, or implementation order."

policy:
  allow_implicit_invocation: true
