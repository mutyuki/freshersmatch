interface:
  display_name: "Freshers Match Admin UI"
  short_description: "Desktop admin and ranking monitor surfaces"
  brand_color: "#1A1A1A"
  default_prompt: "Use $freshers-match-admin-ui to build or refine the desktop operator dashboard, management screens, and ranking monitor."

policy:
  allow_implicit_invocation: true
